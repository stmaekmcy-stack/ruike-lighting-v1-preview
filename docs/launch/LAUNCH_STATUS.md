# 瑞客照明官网 V1.0｜上线状态

> 最后更新：2026-09-20<br>
> 当前阶段：2026-09-20 19:02（中国标准时间）正式主域名已上线并开放索引；最终公网 HTTPS、路由和三种尺寸浏览器交互复验通过。<br>
> 正式上线版本：`be80b05819053ea0ec99af35f78e5312c5b98cf3`；生产运行 `35506459749` 成功，公网 `/healthz` 与服务器 current 均匹配。真实联系方式/收件端与真机验收仍有明确未完成边界。

## 可查看环境

- 预发布：<https://stmaekmcy-stack.github.io/ruike-lighting-v1-preview/>
- 预发布状态：可公开访问，强制 `noindex, nofollow`，不作为正式官网域名。
- 正式主域名：`ruikelight.com`，已购买并完成实名认证。
- 正式网址：<https://ruikelight.com/>，已在本地浏览器、直接 HTTPS 请求、GitHub 生产工作流及上海服务器回验；HTTP 与 www 均 301 到唯一 HTTPS 主域名。
- 存档标签：`v1.0.0-live-20260920` 为正式索引版；`checkpoint-live-https-20260920` 为已验证的隔离首发版。服务器 previous 指向 `0cbe1fc9396604ffe46195e4d54350889244c688`，保留回滚路径。
- 品牌保护域名：`ruikelight.cn`，已购买并备案，网站备案号为“沪ICP备2024099975号-4”；解析、证书与保护跳转尚未配置。
- 中国大陆生产服务器：腾讯云轻量应用服务器，上海，2 核 / 2 GB / 40 GB，公网 IPv4 `124.220.205.94`，运行中，2027-09-04 到期。

## 已完成

- 冻结已通过的首页结构、文案、导航、Logo 与桌面/移动视觉母版。
- 固定 Node.js、npm 及前端依赖版本，保留可复现的锁定文件。
- 字体已本地托管，不依赖 Google Fonts；首屏图增加 WebP 与 PNG 兼容回退。
- 已建立 title、description、canonical、Open Graph、JSON-LD、robots 与 sitemap 的预发布/生产切换机制。
- 已建立隐私说明、网站使用条款、404 页与 favicon。
- 已建立同源项目咨询接口：服务端校验、字段清理、蜜罐、基础频率限制、SMTP 投递、明确成功/失败响应。
- 未配置真实接收端时，生产页不渲染表单，不会出现“看似能提交、实际无人接收”的状态。
- 已建立 GitHub 质量门、预发布流程、手动生产发布流程、不可变发布包、原子切换与一键回滚。
- 已为服务器绑定独立 ED25519 部署密钥，并从本地完成 `ruike-deploy` 密钥登录校验。
- 已在服务器安装 Node.js `v22.23.2`、npm `10.9.8`、Nginx `1.24.0` 和 Certbot `2.9.0`。
- 用户已在管理员终端完成新版运维安装，截图显示旧文件已备份；SSH 核验发布/回滚脚本、systemd 单元与 Nginx 待启用配置的 SHA256 均与审核版本一致，文件由 root 持有。无需重复安装。GitHub `production` environment 的 SSH 配置已齐备。
- DNS、TLS 与最终上线确认就绪前，Nginx 与项目咨询服务保持 `disabled/inactive`。
- 用户于 2026-09-18 提供腾讯云“互联网信息服务”列表截图，确认 `ruikelight.com` 为“沪ICP备2024099975号-5”、`ruikelight.cn` 为“沪ICP备2024099975号-4”，两个网站状态均为“正常”。无需重复提交备案。
- 备案主体为“上海瑞客莱照明有限公司”，主体备案号为“沪ICP备2024099975号”。生产网站使用含后缀的完整网站号，不以主体号或公安联网备案数据码代替。
- GitHub `production` 环境已配置 `COMPANY_ICP_NUMBER=沪ICP备2024099975号-5` 与 `COMPANY_LEGAL_NAME=上海瑞客莱照明有限公司`，并读取回验。`.cn` 的 `-4` 单独登记，不混入 `.com` 页脚。
- 使用真实生产变量完成 lint、typecheck、8 项接口测试及 build；验证首页构建代码、隐私页、条款页和 404 页的 `-5` 备案号及工信部链接正确。canonical、企业结构化数据、robots、sitemap 与版本健康文件均通过静态检查，无开发占位文字。
- 完整 ICP 网站号的生产构建硬门禁继续保留；索引保持关闭，未配置真实接收端的表单继续隐藏。
- 公安联网备案数据码未写入代码仓库或公开文档；待正式网站开通后 30 日内由企业负责人按官方流程办理。
- 正式项目与产品图继续受 `published/draft` 真实性闸门管理；未核验内容不进入生产页。
- 已修复微信复制 API 拒绝后的兼容回退、手机菜单 CTA 的滚动时序、并发重复提交和非业务成功响应被误判为提交成功的问题；没有调整视觉母版。
- 项目咨询接口有 8 项自动化测试，覆盖字段格式、蜜罐、频率限制、来源验证、收件人接受校验、投递失败与个人信息不泄漏；这些是模拟测试，不代表真实邮箱已收到咨询。
- 已在上海服务器使用临时证书与仅回环端口验证生产 Nginx 模板：首页、法律页面、静态资源、404、安全响应头、跳转、版本健康检查和未接通接口的 503 响应均通过。正式 Nginx 与咨询服务仍为 inactive。
- `.cn` 备案前置条件已满足；首发 Nginx 与证书范围仍保持 `.com` / `www`，`.cn` 的保护跳转单独配置，不阻塞主站首发。
- 用户已保存 `.com` 的 `@ A 124.220.205.94` 与 `www CNAME ruikelight.com`（默认线路、TTL 600）。`storm.dnspod.net`、`lepus.dnspod.net` 均返回正确记录；公共解析器和上海服务器也解析到 `124.220.205.94`。不再要求重复添加 DNS。
- 用户此前在管理员终端申请证书并自行确认协议。2026-09-20 Codex 使用已有管理员会话执行已审核脚本，证书链、两个域名、私钥匹配及有效期实测通过；有效期至 2026-12-18 22:44:50 UTC。私钥未离开 root 进程，证书账号邮箱未作为公开联系方式。
- 首次 staging 续期在证书服务最终取回阶段返回 404；脚本已自动恢复配置并保留备份。读取错误后受控重试一次，于 18:20 通过真实 staging 演练，续期已切换 webroot，重载钩子已保存，Certbot timer 为 enabled/active。最终 Nginx 配置与已审核模板哈希一致，`nginx -t` 通过。证据见服务器 `/var/lib/ruike-lighting/https-readiness.txt`；成功备份目录为 `/etc/ruike-lighting/ops-backups/https-hnp2Hh`。
- 已整合 GEO PR #8 的六个知识页、预渲染与校验流程，解决的唯一合并冲突是 package.json 测试入口：同时保留 24 项接口/运维测试与 5 项 GEO 测试。本地真实生产变量下 lint、typecheck、29 项测试和 build 均通过，PR #9 CI 通过后正常合并；没有覆盖已确认页面视觉。
- 生产运行 `35504944104` 已通过构建、运维哈希校验、上传与激活，但公网 HTTPS 检查超时；自动回滚成功，首发文件留存在 `releases/de4b68c7f20e0eba7563289abba5000d509c91a8`，未把失败发布标记为上线。
- 已在腾讯云实例 `lhins-d6pxv3ie` 的防火墙中核对原因：原来只有 TCP 22、TCP 80 和 ICMP。现新增一条 HTTPS / TCP 443 / 全部 IPv4 / 允许，保存后列表回验为 4 条；没有删除原规则、放行其他端口或扩大部署账号权限。新的不可变版本发布后仍须验证公网 HTTPS 与健康版本号。
- 443 规则修复后的生产运行 `35506036575` 全部通过，Nginx 为 enabled/active；UFW 原为 inactive，仅做只读核对，没有关闭防火墙。证书定时器 enabled/active，staging 续期已经真实通过。
- 正式公网的 14 个页面/资源路由返回 200，HTTP/www 3 种入口正确 301，缺失页面、缺失资源及内部文档路径返回 404。未接收咨询的接口返回明确 503，不伪造提交成功。
- 使用实际安装的 Chrome 对正式域名进行 1440、390、430 宽度验收：首屏、导航/手机菜单、产品区的发起项目均进入当前咨询区；返回顶部与七步展开状态切换正常；所有锚点存在、图片加载成功、无横向溢出、console/pageerror/资源错误均为 0。五项与七步数量、标题断行及生产隐藏规则通过。事件队列记录 view_home、click_start_project、view_wechat_qr；未配置统计平台，不宣称后台经营报表已接通。
- 最终索引版生产运行 `35506459749` 成功，再次通过以上全部公网与三尺寸交互验收；lint/typecheck/build、24 项接口/运维测试、5 项 GEO 测试全通过，构建检查 10 个 HTML、7 组结构化数据、128 个内部链接/资源及 9 个 sitemap URL。正式 robots Allow、首页 index/follow、llms 品牌导航已生效；只是允许抓取，不代表搜索或 AI 平台已经收录/推荐。
- 预览工作流 `35506437537` 成功，预览 robots 仍为 Disallow、与正式版本同 SHA，不作为正式品牌信源。`src`/`public` 相比已合并 GEO 基线无差异，本轮未重做页面。

## 当前真实内容状态

| 项目 | 当前状态 | 生产行为 |
| --- | --- | --- |
| 微信二维码 | 已有并继续使用现有官方素材 | 显示 |
| 微信号 | 未提供 | 不显示，不显示复制按钮 |
| 电话 | 未提供 | 不显示 |
| 邮箱 | 未提供 | 不显示 |
| 企业全称 | 腾讯云备案截图核验为“上海瑞客莱照明有限公司”，已配置生产变量 | 已进入生产构建的企业结构化数据 |
| 对外地址 | 当前候选值未确认可公开 | 不显示，不写入结构化数据 |
| 项目表单接收端 | 未配置 | 不显示表单，仅保留已有真实联系入口 |
| 真实项目 | 暂无同时满足核验、授权、`published=true` 的内容 | 项目区与项目导航在生产环境隐藏 |
| 正式产品图 | 尚未补齐高清授权素材 | 仅保留产品能力表达，不公开 draft 图 |

## 当前仍未完成

1. 技术上线和正式索引开放已经完成，无待执行管理员命令或扫码。现有 SSH 发布账号仍只可 sudo 发布/回滚，没有扩权；首次上线准备脚本不得在网站启用后重复执行。
2. 补充真实对外电话、可添加的微信号；现有公众号二维码不能代替手机端客服咨询入口。邮箱可选；如需表单，必须再配置真实收件端并验证实际收到。
3. 后续正式内容发布继续沿用核验、授权、published 闸门；预览环境始终不开放索引。当前没有正式统计平台 ID，事件结构已验证，但没有后台经营结果统计。
4. 真机 iPhone、Android、微信聊天内打开及复制/拨号测试尚未完成；后续桌面浏览器与尺寸模拟测试会单独记录，不能代替真实硬件及微信内测试结论。
5. `.cn` 的 DNS、TLS 与保护跳转尚未配置；其备案已经通过，不得再次列为“备案待核验”。

## 恢复执行的下一动作

正式主域名、索引和预览隔离均已完成。下一步只处理真实咨询信息/接收端、真机与微信内验证以及单独的 `.cn` 保护跳转，不再自行优化视觉。不要重复初始化、申请证书、添加 `.com` DNS 或执行初次准备脚本。证据及未验证边界见 [VERIFICATION.md](VERIFICATION.md)。
