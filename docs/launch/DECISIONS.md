# 上线决策记录

| 编号 | 已生效决策 | 原因/边界 |
| --- | --- | --- |
| D-001 | 首页桌面端与移动端视觉母版冻结 | 本轮只做上线、接收、运维与验收，不重新设计 |
| D-002 | GitHub 作为唯一源代码真源 | 发布必须对应可追溯 commit/tag，服务器不直接修改代码 |
| D-003 | GitHub Pages 仅作为预发布 | 强制 noindex，不承担中国大陆正式站和备案主体 |
| D-004 | 生产架构采用腾讯云中国大陆服务器 + Nginx + HTTPS | 与 ICP 备案、国内访问及静态站运维目标一致 |
| D-005 | 主域名采用 `ruikelight.com`，同步持有 `ruikelight.cn` | 两个域名已购买；`.com` 承担正式官网，`.cn` 作为品牌保护并跳转至主域名 |
| D-006 | 真实联系方式通过生产变量注入 | 缺失电话、微信号或邮箱时直接不渲染，不在代码中编造 |
| D-007 | 表单仅在真实收件与 SMTP 配置完成后开启 | 未配置时整个表单不在生产页显示，网站仍可仅凭真实联系渠道上线 |
| D-008 | 项目与产品内容继续使用真实性闸门 | 未核验、未授权或 draft 内容不进入生产页 |
| D-009 | 生产发布采用手动确认字串 + GitHub production environment | 防止意外发布；用户最终确认前不执行 |
| D-010 | 发布使用不可变目录、原子软链接切换和 previous 回滚 | 新版健康检查失败时自动恢复上一版 |
| D-011 | 预发布与生产的 robots、canonical、sitemap 由构建环境生成 | 避免 GitHub Pages 被收录，同时防止保留错误正式域名 |
| D-012 | 统计平台 ID 不阻塞上线 | 已保留统一事件出口，不写 Cookie，不持久化表单内容，后续可对接正式平台 |
