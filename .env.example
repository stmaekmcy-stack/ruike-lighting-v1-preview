# Public build configuration
VITE_BASE_PATH=/
VITE_PROJECT_FORM_ENDPOINT=
VITE_FORM_SIMULATE_FAILURE=false

# Server-only mail configuration. Never prefix these values with VITE_.
CONTACT_RECEIVER_EMAIL=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
